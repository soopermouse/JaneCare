services:
  janecare:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      - janecare-data:/app/data
volumes:
  janecare-data:
